const darkMode = document.getElementById("darkMode");
const elementBody = document.getElementById("body");
const elementLabel = document.getElementById("label");

function marcado() {
    if (true) {
        elementBody.addClass('darkMode')
    } else if (false) {
        return
    }
}